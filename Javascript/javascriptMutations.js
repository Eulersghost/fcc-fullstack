/* 1. compare the array before and after
2. after arrays change compare it to the previous one
3. if they're different output either true or false, 
    in the freecodecamp example elements in both arrays match true if all there elements are the same, for example ["Hello", "hello"] = true, case sensitivity doesn't matter
4. loop through each element in the array, sidenote probably use the length and split properties/methods
5. 
 */